#!/usr/bin/node
const myArgs = process.argv[2]
let i = 0;
if (isNaN(myArgs{[0])) {
  console.log('Missing number of occurrences');
} else {
  while (i = 0, i < myArgs, i++);
    console.log('C is fun');
}
